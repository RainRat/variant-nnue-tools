name: ffishjs

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

env:
  EM_VERSION: 1.39.16
  EM_CACHE_FOLDER: 'emsdk-cache'

jobs:
  test:
    runs-on: ubuntu-24.04

    strategy:
      matrix:
        node-version: [20.x]

    steps:
      - uses: actions/checkout@v7
      - name: Setup cache
        id: cache-system-libraries
        uses: actions/cache@v6
        with:
          path: ${{env.EM_CACHE_FOLDER}}
          key: emsdk-${{env.EM_VERSION}}-${{ runner.os }}
      - uses: mymindstorm/setup-emsdk@v16
        with:
          version: ${{env.EM_VERSION}}
          actions-cache-folder: ${{env.EM_CACHE_FOLDER}}
      - name: Use Node.js ${{ matrix.node-version }}
        uses: actions/setup-node@v6
        with:
          node-version: ${{ matrix.node-version }}
      - name: Build ffishjs
        working-directory: src
        run: make -f Makefile_js build debug=yes
      - name: Install dependencies
        working-directory: tests/js
        run: |
          if [ -f package-lock.json ]; then npm ci; else npm install; fi
      - name: Run unit tests
        working-directory: tests/js
        run: npm test
      - name: Build very-large-board ES6 module
        working-directory: src
        run: make -f Makefile_js clean && make -f Makefile_js build verylargeboards=yes es6=yes
      - name: Smoke test very-large-board ES6 module
        working-directory: tests/js
        run: |
          node --input-type=module <<'JS'
          import { createRequire } from 'node:module';
          import { dirname } from 'node:path';
          import { fileURLToPath } from 'node:url';
          globalThis.require = createRequire(import.meta.url);
          globalThis.__dirname = dirname(fileURLToPath(import.meta.url));
          globalThis.fetch = undefined;
          const { default: createFfish } = await import('./ffish.js');
          const ffish = await createFfish();
          const board = new ffish.Board('omega');
          if (board.fen() !== 'w**********w/*crnbqkbnrc*/*pppppppppp*/*10*/*10*/*10*/*10*/*10*/*10*/*PPPPPPPPPP*/*CRNBQKBNRC*/W**********W w KQkq - 0 1')
            throw new Error(`Unexpected omega start FEN: ${board.fen()}`);
          board.delete();
          JS

  build:
    runs-on: ubuntu-24.04
    needs: [test]    # Building process must start after successful testing process

    strategy:
      matrix:
        node-version: [20.x]

    steps:
      - uses: actions/checkout@v7
      - name: Setup cache
        id: cache-system-libraries
        uses: actions/cache@v6
        with:
          path: ${{env.EM_CACHE_FOLDER}}
          key: emsdk-${{env.EM_VERSION}}-${{ runner.os }}
      - uses: mymindstorm/setup-emsdk@v16
        with:
          version: ${{env.EM_VERSION}}
          actions-cache-folder: ${{env.EM_CACHE_FOLDER}}
      - name: Use Node.js ${{ matrix.node-version }}
        uses: actions/setup-node@v6
        with:
          node-version: ${{ matrix.node-version }}
      - name: Build ffish.js ES6/ES2015 module
        working-directory: src
        run: rm -f ../tests/js/ffish.js & rm -f ../tests/js/ffish.wasm & make -f Makefile_js build es6=yes
      - name: Upload ffish.js ES6/ES2015 module ZIP archive
        uses: actions/upload-artifact@v7
        with:
          name: ffishjs-es6
          path: tests/js/*
          if-no-files-found: error
          compression-level: 9
      - name: Build ffish.js standard module
        working-directory: src
        run: rm -f ../tests/js/ffish.js & rm -f ../tests/js/ffish.wasm & make -f Makefile_js build
      - name: Upload ffish.js standard module ZIP archive
        uses: actions/upload-artifact@v7
        with:
          name: ffishjs-standard
          path: tests/js/*
          if-no-files-found: error
          compression-level: 9
